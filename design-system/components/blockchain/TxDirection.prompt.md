**TxDirection** — in/out chip. `in` is green (received); `out` is neutral grey (sending isn't an error). Arrow + label.

```jsx
<TxDirection direction="in" />
<TxDirection direction="out" label="Sent" />
```
