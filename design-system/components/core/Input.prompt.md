**Input** — single-line field. Always pass `numeric` for amounts so figures are mono + `tabular-nums` and mobile gets a decimal keypad.

```jsx
<Input placeholder="Search address or hash…" />
<Input numeric placeholder="0.00" />
```
